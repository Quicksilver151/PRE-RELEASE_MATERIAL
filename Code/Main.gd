extends MarginContainer

func _ready():
	pass

#func _process(delta):
#	for node in $Margin/VBoxContainer/DownJourneys.get_children():
#		if node is MarginContainer and node.get_node("Button").disabled:
#			$Margin/VBoxContainer/UpJourneys.get_child(node.get_index()).get_node("Button").disabled = true
